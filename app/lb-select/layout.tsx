export const metadata = {
  title: "Leaderboards",
  description: "Browse JDQ and JVQ global leaderboards.",
};

export default function LeaderboardSelectLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
